#!/bin/sh
/sbin/ip link set dev $INTERFACE up
/sbin/ip addr add dev $INTERFACE {{ icvpn_ipv4_addr }} broadcast 10.207.255.255 scope link
/sbin/ip -6 addr add dev $INTERFACE {{ icvpn_ipv6_addr }} preferred_lft 0
